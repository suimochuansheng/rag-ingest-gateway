# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv_phase1/

# 环境变量
.env

# 日志（关键）
logs/
*.log

# IDE
.vscode/
.idea/

# pytest / 缓存
.pytest_cache/

# 测试报告（pytest / score threshold 等生成的输出产物）
tests/test_report/

# 运行产物 / 调试产物（每次运行生成的带时间戳快照与 docling 调试输出）
phase1/snapshots/
phase1/debug_cleaned/

# 手动测试图片（未被测试引用，避免大文件入库）
phase1/data/images/

# 误建的临时文件（.enva 疑似 .env 的笔误、含密钥片段；aaa.py 为草稿，均不入库）
phase1/.enva
phase1/src/loaders/aaa.py
