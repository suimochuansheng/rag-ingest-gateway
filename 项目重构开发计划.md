##  0阶段计划：数据库奠基与存储模型重构（Day 1）

**🎯 目标**：为"可靠回滚"和"父子索引"准备好存储层。

- **核心任务**：
  1. 新建 `pipeline_jobs` 任务状态表（存储 UUID、状态、风险标签）。
  2. **关键修正**：对现有的 `knowledge_embeddings` 向量表进行 DDL 变更，新增 `job_id`（UUID）和 `parent_id`（UUID）、`chunk_type`（parent/child）字段。
- **为什么这样做**：
  - **解决痛点（数据一致性）**：之前如果向量写入一半失败，无法回滚。通过强制每一条向量带上 `job_id`，后续无论失败在哪个环节，都能执行 `DELETE FROM table WHERE job_id=$1`，实现**原子级的任务清理**。
  - **预留算法升级**：新增的 `parent_id` 和 `chunk_type` 是为"父子索引"预留的字段，这一步是物理层面的基础。

```tex
╔══════════════════════════════════════════════════════════════╗
║  阶段 0 可行性评估：有条件可行（需注意跨项目协调）           ║
╚══════════════════════════════════════════════════════════════╝

【现状梳理】knowledge_embeddings 表目前被 2 个项目、3 处代码
共同读写：
  ① rag_data_dispose/phase1/src/storage/vector_store.py
      → VectorStore.ensure_table() 负责建表，insert_many() 写入
  ② AdaptiveSearchAgent/src/tools/rag.py
      → search_knowledge() 执行向量检索
  ③ AdaptiveSearchAgent/scripts/cleanup_vectors.py
      → 按 kb_id / source_file 清理数据
三处引用使用不同的连接方式（直接 AsyncConnection vs 连接池），
且列名硬编码在各处 SQL 中。

【任务1：新建 pipeline_jobs 表】 可行
建议：AdaptiveSearchAgent 已有 task_states 表（idle / running /
interrupted / completed / failed），可考虑直接复用并扩展 task_states，
增加 risk_label、job_type 等字段，避免表碎片化。
如果坚持独立建表，建议放入主服务的 checkpointer 初始化流程中统一管理。

【任务2：knowledge_embeddings DDL 变更】 需协调修改
影响面完整分析：
  ├─ VectorStore.ensure_table()
  │    → CREATE TABLE 需包含新列（含 DEFAULT NULL 兼容存量）
  ├─ VectorStore.insert_many()
  │    → INSERT 语句需写入 job_id / parent_id / chunk_type
  ├─ search_knowledge()
  │    → SELECT 暂时无影响，但阶段 3 需加 WHERE chunk_type='child'
  ├─ cleanup_vectors.py
  │    → DELETE / SELECT 可增加 job_id 过滤能力
  └─ 存量数据
       → ALTER TABLE ADD COLUMN ... DEFAULT NULL（不破坏现有查询）

关键风险：两个项目共用同一 PostgreSQL 数据库同一张表，
DDL 变更顺序必须严格：先 ALTER 存量表 → 再同步更新两边代码。
若先改代码 VectorStore.ensure_table() 会尝试 CREATE TABLE 覆盖既有表，
可能导致冲突（当前用 IF NOT EXISTS，但列定义变化后行为不确定）。

【API 中的潜在 Bug】
api_main.py:115 调用了 ensure_knowledge_embeddings_table()，
但该函数在当前代码中未被定义（只定义了 ensure_task_states_table）。
建议在阶段 0 先行修复：或补齐该函数，或统一到 checkpointer 初始化中。

【工时评估】1 天紧张但可实现（建议预留 1.5 天含联调验证）。
```

------

## 0阶段总结：Day 0.5 ~ Day 1（代码修复与数据库奠基）

### 一、Day 0.5：修复断裂的流水线（打通 Chunk → Embed → Store）

| 维度         | 详细说明                                                     |
| :----------- | :----------------------------------------------------------- |
| **核心问题** | 原有的 `ingest_knowledge.py` 中，Chunk（切块）、Embed（向量化）、Store（入库）的代码全部被注释（111-159行），流水线只执行到 `after_load` 快照就终止了。 |
| **解决方案** | 将注释代码全部恢复，并**抽离封装**为独立的异步编排函数 `run_full_pipeline`。 |
| **文件路径** | `rag-ingest-gateway/phase1/scripts/ingest_knowledge.py`      |
| **新增内容** | ① 新增 `async def run_full_pipeline(...)` 函数，承载切块→向量化→入库→检索验证的完整链路。 ② 新增 `after_chunk` 和 `after_embed` 两个快照阶段（原只有 `after_load`），提升可观测性。 |
| **调整内容** | ① `main()` 函数在 `after_load` 快照后，改为调用 `await run_full_pipeline(...)` 继续执行下游逻辑。 ② `run_full_pipeline` 内部显式实例化 `SemanticChunker`、`OllamaEmbedder`、`VectorStore`（此阶段暂未做依赖注入改造，保持功能优先）。 |
| **验收标准** | 运行 `python scripts/ingest_knowledge.py -f data/sample.md` 后，`knowledge_embeddings` 表中能查到新增的向量数据，且 `snapshots/` 目录下生成 `after_chunk` 和 `after_embed` 快照。 |

------

### 二、Day 1：数据库奠基（新增 pipeline_jobs 任务状态表）

| 维度         | 详细说明                                                     |
| :----------- | :----------------------------------------------------------- |
| **核心目标** | 为后续的“异步任务管理”和“审计门禁（HITL）”提供存储基础，新增一张独立的任务状态表 `pipeline_jobs`。 |
| **文件路径** | `rag-ingest-gateway/phase1/src/storage/vector_store.py`      |
| **新增内容** | ① 在 `VectorStore` 类中新增方法 `async def ensure_pipeline_jobs_table(self)`，DDL 包含： 　- `id` (UUID 主键)、`kb_id`、`source_file`、`file_sha256`、`status`、`risk_flags` (JSONB)、`estimated_tokens`、`error_message`、`snapshot_path`、时间戳。 　- 同时创建 `file_sha256` 和 `status` 的索引。 |
| **调整内容** | 修正了一个拼写错误：原方法中错误地调用了不存在的 `_get_conn`，现已统一改为 `_get_connection`（与 `ensure_table` 保持一致）。 |

| 维度         | 详细说明                                                     |
| :----------- | :----------------------------------------------------------- |
| **文件路径** | `rag-ingest-gateway/phase1/src/db_init.py`（**新建文件**）   |
| **新增内容** | 新建统一数据库初始化模块，导出 `async def ensure_all_tables()` 函数。内部依次调用： ① `await store.ensure_table()`（建向量表） ② `await store.ensure_pipeline_jobs_table()`（建任务表） |
| **设计意图** | 将“建表”逻辑从业务主流程中剥离，调用方只需一句 `await ensure_all_tables()`，无需关心具体有几张表，符合单一职责原则。 |

| 维度         | 详细说明                                                     |
| :----------- | :----------------------------------------------------------- |
| **文件路径** | `rag-ingest-gateway/phase1/scripts/ingest_knowledge.py`      |
| **调整内容** | 在 `main()` 函数中，`loader.load()` 执行完成并生成 `after_load` 快照之后，`run_full_pipeline()` 之前，插入 `await ensure_all_tables()`，确保所有表存在后再执行后续写入。 |

------

### 三、最终目录/文件变更清单（一目了然）

| 操作类型 | 文件路径                             | 变更说明                                                     |
| :------- | :----------------------------------- | :----------------------------------------------------------- |
| **修改** | `phase1/scripts/ingest_knowledge.py` | ① 恢复并封装 `run_full_pipeline`；② 新增 `after_chunk` / `after_embed` 快照；③ 引入 `ensure_all_tables` 调用。 |
| **修改** | `phase1/src/storage/vector_store.py` | ① 新增 `ensure_pipeline_jobs_table` 方法；② 修复 `_get_conn` → `_get_connection` 拼写错误。 |
| **新增** | `phase1/src/db_init.py`              | 统一数据库初始化入口，封装 `ensure_all_tables`。             |

------

### 四、当前数据库表状态

| 表名                   | 用途                                       | 状态                                             |
| :--------------------- | :----------------------------------------- | :----------------------------------------------- |
| `knowledge_embeddings` | 存储文档向量和文本（供 RAG 检索）          | ✅ 已存在，由 `ensure_table()` 管理               |
| `pipeline_jobs`        | 存储文档摄入任务状态（审计/审批/异步控制） | ✅ 已创建，由 `ensure_pipeline_jobs_table()` 管理 |

------

### 五、当前已具备的能力（阶段性成果）

1. **全链路可运行**：从上传文件到向量入库，整条 Pipeline 已打通。
2. **可观测性增强**：快照覆盖 `after_load`、`after_chunk`、`after_embed` 三个阶段。
3. **数据库基础就绪**：向量表和任务状态表均已存在，为后续异步 API 和审计引擎做好准备。
4. **代码结构清晰**：建表逻辑已抽离至 `db_init.py`，主流程不再臃肿。

## 额外----开始前添加轻量级的日志处理系统

### 📅 日期：2026-07-05

### 🎯 目标

将项目从 `print` 调试模式升级为**生产级日志体系**，支持分级输出、文件持久化、自动轮转和结构化审计，确保线上问题可快速定位。

### 📁 涉及文件与改动

| 文件路径                             | 改动内容                                                     |
| :----------------------------------- | :----------------------------------------------------------- |
| `phase1/scripts/ingest_knowledge.py` | **核心改动**：① 引入 `logging` + `RotatingFileHandler`；② 所有 `print` 替换为 `logger.info/error/warning`；③ 所有异常退出分支增加结构化审计日志（`audit_logger.info(json.dumps(...))`）；④ 正常完成时增加 `ingest_complete` 审计日志。 |
| `phase1/src/storage/vector_store.py` | 将 `insert_many` 中的 `print` 警告替换为 `logger.warning`。  |
| `phase1/.gitignore`                  | 新增 `logs/` 条目，防止日志文件被提交到 Git。                |
| `phase1/logs/`                       | **新建目录**，用于存放 `rag_ingest.log` 及轮转历史文件。     |

### ⚙️ 日志配置核心参数（代码块）

```python
import logging
from logging.handlers import RotatingFileHandler
from pathlib import Path

# 创建 logs 目录
LOG_DIR = Path(__file__).parent.parent / "logs"
LOG_DIR.mkdir(exist_ok=True)
LOG_FILE = LOG_DIR / "rag_ingest.log"

# 统一格式（含文件名、行号、函数名，精准定位）
formatter = logging.Formatter(
    '%(asctime)s - %(name)s - %(levelname)s - %(filename)s:%(lineno)d - %(funcName)s - %(message)s',
    datefmt='%Y-%m-%d %H:%M:%S'
)

# 根日志记录器
root_logger = logging.getLogger()
root_logger.setLevel(logging.DEBUG)

# 控制台 Handler：只显示 INFO 及以上（开发时看关键流程）
console_handler = logging.StreamHandler()
console_handler.setLevel(logging.INFO)
console_handler.setFormatter(formatter)
root_logger.addHandler(console_handler)

# 文件 Handler：记录 DEBUG 及以上（含堆栈详情），自动轮转 10MB/个，保留 5 个历史
file_handler = RotatingFileHandler(
    LOG_FILE, maxBytes=10 * 1024 * 1024, backupCount=5, encoding='utf-8'
)
file_handler.setLevel(logging.DEBUG)
file_handler.setFormatter(formatter)
root_logger.addHandler(file_handler)

logger = logging.getLogger(__name__)
audit_logger = logging.getLogger("audit")   # 独立审计日志记录器
```



### 📊 审计日志（结构化 JSON）

所有任务结束（成功或失败）都会输出一条 JSON 日志，便于后期导入 ELK / Splunk 进行统计分析：

```json
// 成功示例
{"event": "ingest_complete", "file": "sample.pdf", "kb_id": "default", "chunks": 15, "elapsed": 3.2, "status": "success"}

// 失败示例
{"event": "ingest_failed", "file": "sample.pdf", "reason": "unsupported_format", "ext": "xyz", "status": "failed"}
```



### 🛠️ 堆栈捕获方法

在 `except` 块中统一使用 `logger.exception`，自动记录完整调用栈：

```python
try:
    # 可能出错的代码
except Exception as e:
    logger.exception(f"入库失败: {e}")  # 日志文件中会包含完整的 Traceback
```



### ✅ 验收结果

- 运行 `python scripts/ingest_knowledge.py -f data/sample.md` 后，控制台输出清爽，`logs/rag_ingest.log` 文件生成并包含完整的行号、函数名、DEBUG 级别详情。
- Git 忽略生效，日志文件不会误提交。

## 🌟🌟🌟再把基础的loader处理功能添加上:

**在 Day 2 搭建 API 前，花 1 小时把三个 Loader 的“基础文本提取”做扎实，复杂功能（图片、表格、样式）先留空或降级。**





```bash
                                    rag-ingest-gateway/phase1/
                                    
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                        scripts/ingest_knowledge.py                              │
   │                        【主入口】CLI 路由 + 阶段编排                               │
   │                                                                                 │
   │  main()                                                                         │
   │  ├── open(file).read()                          → bytes                         │
   │  ├── ext → Loader 路由                                                          │
   │  ├── loader.load(content)                      → (blocks, doc_meta)  ───┐       │
   │  ├── PipelineSnapshot.save("after_load")        → JSON + TXT 快照        │       │
   │  ├── db_init.ensure_all_tables()                → 建知识库表 + 任务表     │       │
   │  └── run_full_pipeline(blocks, ...)            → 切块→向量化→入库→验证   │       │
   └─────────────────────────────────────────────────────────────────────────┼───────┘
                                                                             │
       ┌─────────────────────────────────────────────────────────────────────┘
       │
       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  阶段① LOAD — 文档解析（按扩展名分派）                                            │
   │                                                                                 │
   │  ┌─ .pdf  ───▶ src/loaders/pdf_loader.py                                        │
   │  │              【PDF解析器】 pypdf逐页提取 → 每页一个Block                        │
   │  │                                                                              │
   │  │  ┌─ .docx ──▶ src/loaders/docx_loader.py                                     │
   │  │  │            【Word解析器】 python-docx段落提取, 表格回退                       │
   │  │  │                                                                           │
   │  │  ├─ .xlsx ──▶ src/loaders/excel_loader.py                                    │
   │  │  │            【Excel解析器】 pandas逐行→"列名:值", 保留Sheet名                  │
   │  │  │                                                                           │
   │  │  └─ .md ────▶ src/loaders/markdown_loader.py ◀◀ 最复杂路径                    │
   │  │  └─ .txt       内部串联: Clean → Caption → Split → BuildBlocks               │
   │  │                                                                              │
   │  └── 所有Loader继承自: src/loaders/base.py                                       │
   │       【抽象基类】 load(bytes) → tuple[List[Block], Dict[DocMeta]]               │
   └─────────────────────────────────────────────────────────────────────────────────┘
             │
             │  仅 Markdown 路径进入清洗阶段
             ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  阶段② CLEAN — Markdown 清洗（4层管道）                                           │
   │                                                                                 │
   │  src/loaders/markdown_loader.py                                                 │
   │    └── self.cleaner.clean(text)                                                 │
   │          │                                                                      │
   │          ▼                                                                      │
   │  src/cleaners/markdown_cleaner.py                                               │
   │  【Markdown清洗器】 集成4层清洗 + 图片提取                                          │
   │    Layer1: 去YAML前导 / HTML注释 / 提取图片→[IMG_N] / 裸URL                       │
   │    Layer2: 标题#后加空格 / LaTeX包裹                                              │
   │    Layer3: 删版权声明 / TODO占位符                                                 │
   │    Layer4: ``` → <CODE_BLOCK>...</CODE_BLOCK>                                   │
   │    输出: (cleaned_text, {image_paths: [...], ...})                              │
   └─────────────────────────────────────────────────────────────────────────────────┘
             │
             │  图片路径列表非空时进入
             ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  阶段③ CAPTION — AI图片描述                                                      │
   │                                                                                 │
   │  src/loaders/markdown_loader.py                                                 │
   │    └── self.captioner.describe_image(url, alt)                                  │
   │          │                                                                      │
   │          ▼                                                                      │
   │  src/vision/image_captioner.py                                                  │
   │  【图片描述器】 DashScope Qwen-VL 异步调用                                         │
   │    _resolve_image_url() → 相对路径→绝对路径                                       │
   │    AioMultiModalConversation.call(model="qwen3.7-plus", messages=[...])         │
   │    输出: 自然语言描述文本（如"[自然照片] 画面展示了..."）                              │
   │    close_session() → 释放 aiohttp 连接池                                          │
   └─────────────────────────────────────────────────────────────────────────────────┘
             │
             ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  阶段④ VALIDATE — 快照验证（3个阶段）                                              │
   │                                                                                 │
   │  src/snapshot.py                                                                │
   │  【Pipeline快照】 旁路非侵入式观测                                                  │
   │    save(stage, file, blocks, meta) → snapshots/{stage}/{name}_{ts}.{json,txt}   │
   │                                                                                 │
   │  调用点:                                                                         │
   │    ingest_knowledge.py:108  → after_load   (Loader产出后, 15 blocks)             │
   │    ingest_knowledge.py:152  → after_chunk  (切块后, M≥N chunks)                  │
   │    ingest_knowledge.py:186  → after_embed  (向量化后, 不含向量值)                  │
   │                                                                                 │
   │  src/db_init.py                                                                 │
   │  【数据库初始化】 建向量表 + 任务表                                                 │
   │    ensure_all_tables() → VectorStore.ensure_table() + ensure_pipeline_jobs_table()│
   │                                                                                 │
   │  src/logger.py                                                                  │
   │  【日志模块】 控制台INFO + 文件DEBUG(RotatingFileHandler, 10MB轮转)                │
   │    get_logger(name) → 全链路统一日志格式                                           │
   │                                                                                 │
   │  src/config.py                                                                  │
   │  【配置中心】 pydantic-settings, 从 .env 加载                                      │
   │    POSTGRES_DSN / OLLAMA_BASE_URL / CHUNK_SIZE / EMBEDDING_MODEL ...            │
   └─────────────────────────────────────────────────────────────────────────────────┘
             │
             ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  阶段⑤ CHUNK — 语义切块                                                          │
   │                                                                                 │
   │  src/chunking/semantic_chunker.py                                               │
   │  【语义切块器】 RecursiveCharacterTextSplitter                                    │
   │    chunk_size=512, chunk_overlap=50                                             │
   │    分隔符: ["\n\n","\n","。","！","？","；","，"," ",""]                           │
   │    短文本(<256字)免切                                                             │
   │    输入: List[{"text","metadata"}]  输出: List[{"content","metadata"}]  ←字段名变化│
   └─────────────────────────────────────────────────────────────────────────────────┘
             │
             ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  阶段⑥ EMBED — 向量化                                                            │
   │                                                                                 │
   │  src/embedding/embedder.py                                                      │
   │  【Ollama嵌入器】                                                                 │
   │    embed_batch([content1, content2, ...])                                        │
   │      → POST localhost:11434/api/embeddings (串行, nomic-embed-text)              │
   │      → 输出: List[List[float]]  (每条768维)                                       │
   │    失败: 零向量 [0.0]*768 占位                                                    │
   └─────────────────────────────────────────────────────────────────────────────────┘
             │
             ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  阶段⑦ STORE — 向量入库                                                          │
   │                                                                                 │
   │  src/storage/vector_store.py                                                    │
   │  【向量存储】 pgvector PostgreSQL                                                │
   │    ensure_table()           → CREATE TABLE knowledge_embeddings + IVF索引        │
   │    ensure_pipeline_jobs_table() → CREATE TABLE pipeline_ingest_jobs (审计)       │
   │    insert_many(chunks, embs)→ 逐条INSERT, 校验 embedding 维度(≠768跳过)           │
   │    search(vector, kb_id)    → cosine相似度 (<=>) Top-K 检索                       │
   └─────────────────────────────────────────────────────────────────────────────────┘
             │
             ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │  阶段⑧ VERIFY — 检索验证                                                         │
   │                                                                                 │
   │  src/embedding/embedder.py (复用)                                                │
   │    embed_text(test_query[:50]) → 查询向量                                        │
   │                                                                                 │
   │  src/storage/vector_store.py (复用)                                              │
   │    search(query_vector, kb_id, top_k=2) → [{content, similarity, ...}]          │
   │                                                                                 │
   │  ✓ 审计日志输出: {"event":"ingest_complete","run_id":"...","chunks":N,...}       │
   └─────────────────────────────────────────────────────────────────────────────────┘
```

![image-20260706111130122](D:\softwarefile\typoraFile\typora_image\image-20260706111130122.png)



## 计划--阶段 1：基础设施治理与并发安全修复（Day 2）

**🎯 目标**：解决致命性能瓶颈，确保接口在高并发下的绝对安全。

- **核心任务**：
  1. **CPU 密集型隔离**：将 `ingest_knowledge.py` 中的 `Loader` 解析逻辑（PDF/Word/MD）剥离为**纯同步函数**，并在 API 调度层使用 `asyncio.to_thread` 将其扔进线程池运行。
  2. **原子化审批（防双击）**：改造 `/approve` 接口，将 SQL 更新语句改为 `UPDATE ... SET status='PROCESSING' WHERE id=$1 AND status='AUDITING'`，并校验受影响行数。
- **为什么这样做**：
  - **解决痛点（系统稳定性）**：FastAPI 是单线程事件循环。如果不隔离，解析一个 100MB 的 PDF 会导致 `/chat/stream` 对话接口超时卡顿。使用线程池后，Web 响应与重型计算**物理隔离**，保证了服务的平滑运行。
  - **解决痛点（幂等性）**：前端网络抖动或用户双击"批准"按钮，会导致同一个 Job 被启动两次，造成向量库数据翻倍。利用数据库**行级锁**的条件更新，确保只有第一个请求能成功更改状态，彻底杜绝重复消费。

```tex
╔══════════════════════════════════════════════════════════════╗
║  阶段 1 可行性评估：核心思路正确，但缺少前置基础设施             ║
╚══════════════════════════════════════════════════════════════╝

【现状关键事实】
rag_data_dispose 当前是一个纯粹的 CLI 脚本项目，没有任何 FastAPI
服务。ingest_knowledge.py 通过 argparse 接收命令行参数，直接调用
asyncio.run(main(...))。这意味着：
  - 不存在 "/chat/stream 对话接口卡顿" 的问题 —— 因为根本没有
    Web 服务；
  - 不存在 "/approve 接口" —— 因为根本没有 API 层；
  - Loader 的 load() 是 async 方法，但内部库（pypdf、python-docx）
    是同步调用，确实会阻塞当前线程。

【任务1：CPU 密集型隔离】 思路正确，但需先搭建 API 服务
具体步骤：
  (1) 【前置依赖】在 rag_data_dispose 中新建 FastAPI 服务，
      暴露 POST /ingest 端点（不是 /chat/stream，那是主服务的）；
  (2) 将 Loader.load() 从 async def 改为纯同步 def（当前内部逻辑
      已经是同步的，改签名即可）；
  (3) 在 API handler 中通过 await asyncio.to_thread(loader.load,
      content) 将解析扔进线程池；
  (4) embedding（调用 Ollama HTTP API）保持真正的异步，不要放入
      to_thread。

当前 Loader 现状确认：
  - DocumentLoader.load() 抽象基类签名是 async def
  - PDFLoader / DocxLoader 实现是 async def，但内部 pypdf /
    python-docx 全部同步执行
  - 这意味着 async 是"虚假的"—— 改成纯同步 + to_thread 完全可行

【任务2：原子化审批】 需要先建 pipeline_jobs 表（阶段 0 产物）
当前 AdaptiveSearchAgent 的审批流是基于 LangGraph interrupt()
机制实现的，不是基于数据库状态机的。计划中的 /approve 指向的
是 rag_data_dispose 侧的新建审批端点，与主服务的 HITL 是两套体系。
建议：
  (a) 审批对象是 pipeline_jobs 表中的任务，不是 LangGraph 的
     checkpoint；
  (b) UPDATE ... WHERE id=$1 AND status='AUDITING' 的模式完全正确；
  (c) 主服务 AdaptiveSearchAgent 的 human_approval 节点不需要改动。

【工时评估】计划 1 天严重不足。建议拆分为 2 天：
  Day 2a：搭建 rag_data_dispose 的 FastAPI 服务骨架 + to_thread 隔离
  Day 2b：实现 /approve + /reject 端点 + 与 pipeline_jobs 联调
```

------

## 总结--阶段1总结：

 



### 阶段 2：智能审计引擎与 HITL 门禁（Day 3）

**🎯 目标**：植入企业级"质检员"，实现成本控制与合规拦截。

- **核心任务**：
  1. 新建 `auditor.py`，实现三道闸门：SHA256 **去重**检测、**敏感词**正则扫描。
  2. **关键修正**：抛弃 `len(text)//4` 的低级估算，引入 `tiktoken` 库（`cl100k_base` 编码），精确计算文档的 Prompt Token 消耗，设定阈值（如 > 50k）触发 `HIGH_COST` 风险。
  3. 串联逻辑：解析完成后若触发风险，任务状态自动挂起为 `AUDITING`，等待 `/approve` 或 `/reject` 接管。
- **为什么这样做**：
  - **解决痛点（成本失控）**：中文场景下，`len//4` 会导致成本被低估 4 倍，财务上不可接受。使用 OpenAI 官方 `tiktoken` 库来计算（仅需 3 行代码），能让你的**成本预估精准度达到 99.9%**，在面试中体现严谨的工程素养。
  - **体现产品思维**：企业宁愿让机器等着，也不愿让垃圾数据污染知识库或烧掉 API 额度。这套 HITL（人机协同）机制直接命中企业对"数据合规"和"成本可控"的刚需。

```tex
╔══════════════════════════════════════════════════════════════╗
║  阶段 2 可行性评估：技术可行性高，但定位需微调                 ║
╚══════════════════════════════════════════════════════════════╝

【现状确认】
当前代码库中不存在 len(text)//4 的 Token 估算。审计模块为零，
SHA256 去重和敏感词扫描均未实现。

【任务1：auditor.py 三道闸门】 可行
(a) SHA256 去重检测：
    - 对上传文件的原始字节内容计算 SHA256
    - 在 pipeline_jobs 表中查询是否已存在相同 hash 的已完成任务
    - 复杂度低，约 20 行代码
(b) 敏感词正则扫描：
    - 需要维护敏感词列表（建议放在独立的 yaml/json 配置文件中）
    - 注意性能：全文档正则扫描较大文档可能耗时，建议只扫描
      解析后的文本而非原始二进制
(c) tiktoken 精确 Token 计算：
    - 安装 tiktoken 库，使用 cl100k_base 编码
    - "仅需 3 行代码"是准确的：enc = tiktoken.get_encoding("cl100k_base")
      → tokens = enc.encode(text) → len(tokens)
    - 需注意 tiktoken 是纯 CPU 操作，放入 auditor 同步函数即可

【任务2：AUDITING 状态挂起】 依赖阶段 0 和阶段 1 的产物
流程串联：
  POST /ingest → Loader 解析 → auditor 审计
     → 无风险：直接进入 chunk+embed+store 流水线
     → 有风险：写入 pipeline_jobs(状态=AUDITING, risk_tags=...)，
       返回给前端"需要审批"，等待 POST /approve 或 /reject

【定位问题】
计划中第三条闸门"Token 成本估算"的动机是防止 API Embedding
调用成本失控。但当前项目使用的 embedding 是本地 Ollama
（nomic-embed-text），不走 OpenAI API，不存在 Token 计费问题。
如果未来切换为 OpenAI text-embedding-3-large 等付费 API，则
该检查才真正生效。建议调整为：
  - 当前阶段：SHA256 去重 + 敏感词扫描（实际生效）
  - tiktoken 成本估算：作为"可插拔闸门"预留，加注释标注适用场景

【工时评估】1 天可实现核心逻辑（auditor.py + 串联），
但建议预留 0.5 天用于敏感词列表的整理和边界测试。
```

------

# 待处理--阶段二完成后，优先考虑为日志收集系统添加`@log_node` 装饰器、Rich 仪表盘、指标收集器





### 阶段 3：RAG 算法升华——父子索引闭环（Day 4）

**🎯 目标**：告别机械切块，引入高级检索策略，形成"问题-方案-指标"的简历闭环。

- **核心任务**：
  1. **入库策略**：修改 `run_chunk_and_store`，按段落生成**父块**（长文本，存入 `parent_content`），再将父块拆分为**子块**（短文本，用于向量检索）。
  2. **检索策略**：修改搜索助手的 `search_knowledge` 函数，让向量只检索 `chunk_type='child'` 的子块。命中后，根据 `parent_id` 反查出完整的父块内容喂给大模型。
- **为什么这样做**：
  - **解决痛点（上下文割裂）**：512 字的机械切块会把"违约责任"和"赔偿金额"切到两段。用户提问时，子块可能包含"赔偿"，但大模型看不到父块的完整逻辑，回答必然残缺不全。父子索引确保了**召回精度（子块匹配）**和**回答广度（父块内容）**的双赢。

```tex
╔══════════════════════════════════════════════════════════════╗
║  阶段 3 可行性评估：核心难点，需先修复当前断裂的流水线         ║
╚══════════════════════════════════════════════════════════════╝

【重大发现：当前 chunking/embedding/store 代码已被注释掉！】
ingest_knowledge.py 第 111-159 行全部被注释：
  - SemanticChunker 实例化 → 被注释
  - chunker.chunk(raw_blocks) → 被注释
  - OllamaEmbedder 向量化 → 被注释
  - VectorStore.insert_many() 入库 → 被注释
当前流水线只运行到 after_load 快照阶段就终止了。
这意味着"父块入库→子块入库"的完整流通路目前是断的。

【任务1：入库策略 —— run_chunk_and_store】
问题：计划中引用的 "run_chunk_and_store" 函数在当前代码中不存在。
当前 chunk + embed + store 逻辑被分散在三处：
  - chunking/semantic_chunker.py（RecursiveCharacterTextSplitter,
    512 chars）
  - embedding/embedder.py（OllamaEmbedder）
  - storage/vector_store.py（VectorStore.insert_many）
需要新建一个编排函数来串联这些步骤，并实现父子切块逻辑：

建议的实现方案：
  (A) 父块生成：按 paragraph（\n\n 分隔）聚合 raw_blocks 中的 text，
      每个父块 1000-2000 字符，存入 knowledge_embeddings
      (chunk_type='parent', parent_id=NULL)
  (B) 子块生成：对每个父块用 RecursiveCharacterTextSplitter 再切为
      512 字符的短块，存入 knowledge_embeddings
      (chunk_type='child', parent_id=父块UUID)
  (C) 向量化：只对子块生成 embedding（父块不生成向量，只存文本），
      节省 embedding 计算和存储开销

关键设计决策：父块存不存向量？
  推荐：父块不存向量（embedding=NULL），只做文本存储。
  子块存向量用于检索召回，父块只作为"上下文窗口"扩展。
  这样每个文档的向量数量不变（仍为子块数），不增加检索延迟。

【任务2：检索策略 —— search_knowledge 改造】
当前 search_knowledge() 的 SQL：
  SELECT title, content, 1-(embedding<=>%s) AS similarity
  FROM knowledge_embeddings ORDER BY embedding<=>%s LIMIT %s
改造为两步查询：
  (1) 向量检索子块：
      SELECT id, parent_id, content
      FROM knowledge_embeddings
      WHERE chunk_type='child'
      ORDER BY embedding<=>%s LIMIT %s
  (2) 根据 parent_id 反查父块内容：
      SELECT content FROM knowledge_embeddings
      WHERE id = ANY($1) AND chunk_type='parent'
  (3) 将父块内容拼接为上下文喂给 LLM

需注意：多个子块可能映射到同一个父块，需对 parent_id 去重，
避免重复查询和上下文冗余。

【SerpAPI 搜索工具的兼容性】
search_knowledge 只处理 knowledge_embeddings 表（RAG 检索），
不影响 Tavily 网页搜索（src/tools/search.py），两者独立。

【工时评估】1 天严重不足。父子索引是本次重构中最复杂的部分：
  - 新编排函数 + 父子切块逻辑：0.5 天
  - VectorStore 入库改造（支持 chunk_type / parent_id）：0.5 天
  - search_knowledge 两步检索改造：0.5 天
  - 端到端验证（入库→检索→LLM 回答质量）：0.5 天
  建议 2 天为合理区间。
```

------

### 阶段 4：量化评测脚本与数据闭环（Day 5）

**🎯 目标**：跑出属于你自己项目的量化指标，告别"凭感觉"。

- **核心任务**：
  1. 准备 50-100 条测试问答对（Q&A Ground Truth）。
  2. 编写离线评测脚本：
     - 分别运行"旧版（固定切块）"和"新版（父子索引）"的检索流程。
     - 计算 **Hit Rate（命中率）** 和 **MRR（平均倒数排名）**。
- **为什么这样做**：
  - **解决痛点（简历空洞）**：这是最具杀伤力的一步。没有量化数据的 RAG 项目在面试官眼里就是玩具。
  - **面试话术底气**：当你说出"引入父子索引后，Hit Rate 从 65% 提升到了 92%，MRR 从 0.52 提升至 0.81，复杂长尾问题的答案完整率提升 55%"时，你**无懈可击**地证明了你的优化是有效的，而非只是把代码跑通。

```tex
╔══════════════════════════════════════════════════════════════╗
║  阶段 4 可行性评估：方向正确，但评测基础设施需从头搭建         ║
╚══════════════════════════════════════════════════════════════╝

【现状确认】
项目当前没有任何评测脚本，没有测试问答对集，也没有 Hit Rate /
MRR 计算的工具函数。AdaptiveSearchAgent 的 tests/ 目录只覆盖
了 agent 流程（planner / evaluator / json_parser / frontend），
完全不涉及 RAG 检索质量的评估。

【任务1：准备 50-100 条测试问答对】 纯人工工作，不可自动化
这是整个计划中唯一无法靠编码解决的部分。建议策略：
  (a) 选取 3-5 篇高质量中文文档（财务报表、合同条款、技术手册）
  (b) 每篇文档构造 15-20 个问答对
  (c) 问题类型覆盖：事实查询 / 跨段落推理 / 长尾细节 / 否定查询
  (d) 每个问答对标注相关的"黄金段落"（正确答案所在的父块范围）
估计人工耗时：3-5 小时（取决于文档复杂度）

【任务2：离线评测脚本】 技术可行
脚本架构建议：
  eval.py
  ├── load_dataset()          → 读取 JSON 格式的 Q&A Ground Truth
  ├── run_retrieval_baseline() → 旧版：固定 512-char chunk 检索
  ├── run_retrieval_parent()   → 新版：父子索引检索
  ├── compute_hit_rate()       → Hit@K（K=1,3,5）
  ├── compute_mrr()            → Mean Reciprocal Rank
  └── generate_report()        → 输出对比表格 + 提升幅度

关键注意点：
  - "旧版" vs "新版"需要两套独立的检索流程，可以通过切换
    search_knowledge 的参数化配置实现（如 chunk_strategy 参数）
  - 不能用同一套向量数据对比 —— 旧版是单一 chunk 表，新版是
    父子双表。需要准备两份独立的 knowledge_embeddings 数据
    （或分不同 kb_id 隔离开）
  - 评测脚本的"黄金段落"匹配逻辑需要仔细设计：由于 chunk 边界
    不可能完美对齐 Ground Truth 段落，建议使用字符重叠率
    （intersection / union）> 0.5 即视为命中

【"旧版"数据留存】
当前 ingest_knowledge.py 的 chunking 代码是注释掉的，如果现在
取消注释按 512-char 固定切块跑一次入库，可以作为"旧版基准"保存。
后续父子索引改造后，用新策略重新入库另一个 kb_id，评测脚本分别
连接两个 kb 进行对比。

【工时评估】1 天可实现脚本框架，但"准备测试集"是前置人工工序。
  建议：
  - 提前 1 天开始构造测试集（与阶段 2/3 并行进行）
  - Day 5 聚焦脚本编写和跑数据
```
