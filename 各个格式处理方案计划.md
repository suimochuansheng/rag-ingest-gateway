## 一、整体方案评估：IR（中间表示）架构

| 维度           | 评价                                                         |
| :------------- | :----------------------------------------------------------- |
| **架构正确性** | ✅ **完全正确**。用 Docling 统一 PDF 和 DOCX 的解析出口（转 Markdown），让下游只面对标准格式，这是典型的“防腐层”设计，是大型企业级 RAG 系统的标配做法。 |
| **面试冲击力** | ✅ **极强**。面试官听到“Docling AST 节点过滤、TableFormer 双栏重排、页眉页脚裁剪”时，能立刻意识到你接触过**真实复杂文档**，而非玩具级别的 `pypdf`。 |
| **工程复杂度** | ⚠️ 需要确认 Docling 在本地环境是否可用（它依赖一些 ONNX 模型，首次运行会下载），以及你是否有时间做完整集成。 |

**我的建议**：**采用 Docling 方案作为 PDF/DOCX 的主力解析器**，但保留一份轻量级备用方案（现有的简化版 Loader）作为降级路径，防止 Docling 在部署环境因网络/模型问题崩溃。

------

## 二、逐条评审你的设计

### 1. MD 格式：直接进入 MarkdownLoader ✅

**完全正确**，不赘述。

------

### 2. PDF 格式：Docling 转 Markdown + 节点过滤 ✅

你的思路：

> 在 Docling 将 AST 导出为 Markdown 的前置阶段，过滤掉类别为 Page-header 和 Page-footer 的节点，然后再渲染为纯净的 Markdown 文本送入下游。

**这非常精准**。Docling 的 `Document` 对象包含 `element_type` 字段，可以在导出前过滤：

python

```
# 过滤示例
from docling.document import Document
filtered_elements = [
    e for e in doc.elements 
    if e.element_type not in ["page-header", "page-footer"]
]
# 重新渲染为 Markdown
```



**唯一需要注意**：双栏检测是 Docling 的默认行为（通过 `Document` 中的 `layout` 信息已经做了版面分析），所以“双栏跨栏乱序”问题本质上已经被 Docling 解决了，不需要额外处理。

**建议**：PDFLoader 采用 Docling 转换，但 OCR 路径仍保留你之前写好的 `pdf2image + pytesseract` 作为降级。

------

### 3. DOCX 格式：Docling 转 Markdown ✅

你描述的流程：

> Docling 直接抓取 DOCX 底层的 XML 标记，将 Heading 1、Heading 2 映射为 `#`、`##`，表格转为 Markdown Table。

**完全符合 Docling 的设计目的**。`docling` 对 DOCX 的解析质量远高于 `python-docx`（它直接解析 OpenXML，支持嵌套样式、复杂段落结构）。

**建议**：DocxLoader 同样改用 Docling 引擎，输出的 Markdown 与 PDFLoader 完全同构，下游 `MarkdownLoader` 可以复用清洗管道。

------

### 4. Excel 格式：行级序列化（策略 A） ✅ + Agentic Text-to-SQL（策略 B）⏳

| 策略                       | 适用场景                         | 当前阶段是否实现     |
| :------------------------- | :------------------------------- | :------------------- |
| **A：行级序列化**          | 向量检索主力，单行精准命中       | ✅ **立即实现**       |
| **B：Agentic Text-to-SQL** | 聚合/统计分析类查询（求和/过滤） | ⏳ **延后作为优化项** |

**策略 A 的细节**：
你描述的“列名: 值”序列化方式是正确的。入库时每行作为一个独立 Chunk，`metadata` 中保留行号、Sheet 名，便于追溯。

**策略 B 的定位**：
这更适合作为 **搜索助手端** 的能力（`AdaptiveSearchAgent` 里的工具路由），而不是摄入端的职责。因为：

- 用户 query 进来后，先判断“是检索还是聚合/分析”。
- 如果是聚合类，执行 SQL/DataFrame 分析。
- 摄入端只负责“把 Excel 结构化数据喂进去”，不负责“分析如何查询”。

所以策略 B 可以作为后续优化项，不在 Day 0.5 的范围。

------

## 三、统一的 IR 架构流程图（最终设计）

text

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          文档摄入统一入口                                   │
│                     POST /ingest (FastAPI)                                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     格式路由 (扩展名判断)                                    │
│  .md → MarkdownLoader (原始)                                               │
│  .pdf → DoclingPDFLoader (Docling → AST → 过滤页眉页脚 → Markdown)        │
│  .docx → DoclingDocxLoader (Docling → Heading映射 → Markdown)             │
│  .xlsx → ExcelRowLoader (Pandas → 行级序列化 → 文本块)                    │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    统一中间表示 (IR)                                        │
│             所有 Loader 输出标准化 Block 列表:                               │
│             {"text": "...", "metadata": {...}}                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    下游统一管道（不变）                                      │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐            │
│  │ Markdown │ →  │ Semantic │ →  │ Ollama   │ →  │ Vector   │            │
│  │ Cleaner  │    │ Chunker  │    │ Embedder │    │ Store    │            │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘            │
└─────────────────────────────────────────────────────────────────────────────┘
```



**关键价值**：下游（切块、向量化、入库）**完全不需要感知文档来源**，无论 PDF/DOCX/Excel 都统一为 `{"text": "...", "metadata": {...}}` 格式。这极大简化了系统复杂度，是架构设计的精髓。

------

## 四、Day 0.5 的修正计划（基于 IR 架构）

| 步骤 | 操作                                                         | 文件                  |
| :--- | :----------------------------------------------------------- | :-------------------- |
| 1    | 安装 Docling：`pip install docling`                          | 终端                  |
| 2    | 新建 `loaders/docling_pdf_loader.py`：继承 `DocumentLoader`，内部用 Docling 解析 PDF，AST 过滤后转 Markdown 文本 | 新建                  |
| 3    | 新建 `loaders/docling_docx_loader.py`：同上，专门处理 DOCX   | 新建                  |
| 4    | 修改 `ExcelLoader`：改为行级序列化（替换原来的简单提取）     | `excel_loader.py`     |
| 5    | 在 `ingest_knowledge.py` 路由表中，PDF 指向 `DoclingPDFLoader`，DOCX 指向 `DoclingDocxLoader` | `ingest_knowledge.py` |
| 6    | **保留原 PDFLoader/DocxLoader**（重命名为 `pdf_loader_legacy.py` 等），作为降级备用 | 重命名，不删除        |

------

## 五、需要注意的风险（提前告诉你）

| 风险                                                         | 应对                                                         |
| :----------------------------------------------------------- | :----------------------------------------------------------- |
| **Docling 首次运行下载模型**（几百 MB，取决于网络）          | 提前在开发环境跑一次“预下载”，或标注在启动文档中             |
| **Docling 处理大文件时内存占用高**                           | 如果在 Day 2 的 FastAPI 中跑，必须放入 `asyncio.to_thread` 隔离 |
| **Docling 输出的 Markdown 可能包含图片占位符**               | 可以通过 `doc.options.image_to_text = False` 关闭图片描述，或保持留空（你的 MD 清洗管道可以识别并跳过） |
| **Excel 行级序列化后，Chunk 数量可能剧增**（一个万行 Excel 就是一万个 Chunk） | 增加一个 `max_rows` 配置，限制单个 Excel 的处理上限（比如 5000 行），超出则截断并告警 |

------

## 六、总结

| 你的设计                                     | 审核结论                           |
| :------------------------------------------- | :--------------------------------- |
| IR 统一架构（MD + Docling + Excel 行序列化） | ✅ 通过，是最佳实践                 |
| PDF 用 Docling 转 Markdown + 过滤页眉页脚    | ✅ 通过，精准解决双栏/表格/噪声问题 |
| DOCX 用 Docling 转 Markdown + 标题映射       | ✅ 通过，比 `python-docx` 质量更高  |
| Excel 策略 A（行级序列化）                   | ✅ 立即实现                         |
| Excel 策略 B（Agentic Text-to-SQL）          | ⏳ 延后到搜索助手端实现             |

**你可以按上述计划执行**。如果 Docling 集成过程中遇到任何问题（模型下载失败、导入报错等），把错误信息发我，我协助解决。